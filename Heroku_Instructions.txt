heroku create <your appname>
heroku git:remote -a your-app
heroku buildpacks:set heroku/nodejs
git push heroku main
heroku ps:scale web=1
git push heroku main


heroku plugins:install heroku-builds
heroku plugins:install heroku-repo

heroku apps:destroy -a configvars --confirm configvars
heroku repo:reset -a ammhelloworld
heroku builds:cache:purge -a ammhelloworld --confirm ammhelloworld
heroku builds:cache:purge -r ammhelloworld --confirm ammhelloworld
git commit --allow-empty -m "Purge cache"
git push heroku main


heroku ps <app>


Heroku local
git remote add origin ammhelloworld
git pull -r origin main
git push -u origin main


git init
git add .
git rm -rf spec/ --cached
git commit -m "init"
git remote add heroku git@heroku.com:ammhelloworld.git
echo "Added heroku remote"
#pull heroku but then checkback out our current local master and mark everything as merged
git pull heroku main
git checkout --ours .
git add -u
git commit -m "merged with github main"
echo "merged with github"
#push back to heroku, open web browser, and remove git repository
git push heroku main
echo "Now you should check if heroku build is working fine"

# remove heroku .git folder
rm -fr .git

# Go back where we started
echo "Everything Done"